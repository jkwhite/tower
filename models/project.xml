<?xml version="1.0"?>
<project>
  <extend>../project.xml</extend>

  <dependencies>
    <dependency>
      <groupId>excelsiorg</groupId>
      <artifactId>tower</artifactId>
      <version>${pom.currentVersion}</version>
      <properties>
        <jar.manifest.classpath>true</jar.manifest.classpath>
      </properties>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>${basedir}/src/res</directory>
        <includes>
          <include>**/*.jme</include>
        </includes>
      </resource>
    </resources>
  </build>
</project>
